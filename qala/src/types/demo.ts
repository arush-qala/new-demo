export type Brand = { slug: string; name: string; tagline?: string }
export type Collection = { slug: string; name: string; cover: string }
export type Product = { slug: string; name: string; price: number; cover: string }

